services:
  postgres-vdb:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: p_rag_matic_db
    env_file:
      - ../.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata: